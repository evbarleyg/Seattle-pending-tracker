# macOS
.DS_Store

# Node / Vite
node_modules/
dist/
.vite/

# Keep scratch refresh outputs local, except the generated 2026 pending-context deliverable.
tmp/*
!tmp/sold_2026_pending_context.csv
!tmp/sold_2026_pending_context_missing.csv
!tmp/sold_2026_pending_context_report.json

# Raw downloads and extracted bulk county datasets (too large for repo)
*.zip
EXTR_*.csv
Value History/

# Keep the app-ready derived dataset
!public_sales_proxy_all_prices_last12mo.csv
!public_sales_proxy_mls_enriched_last12mo.csv

# Local realtor exports used to build enrichment (do not commit)
realtor_exports/

# Local Codex export artifacts
codex_context_export_sanitized/
codex_context_export_structured_sanitized/

# Local Claude Code preferences and experiments
CLAUDE.local.md
.claude/settings.local.json

# Redfin scrape outputs (regenerated from API)
redfin_active_listings.csv
redfin_sold_listings.csv
redfin_fetch_report.json
redfin_sold_report.json
redfin_merge_report.json
redfin_url_index.json
redfin_history_cache.json
redfin_backfill_report.json
redfin_transitions_report.json
redfin_active_snapshots/

# Private financial / affordability config (real balances, income, equity).
# NEVER commit or sync to public/. The sample schema is tracked; the real
# config is local-only and absent from CI builds, so the public deploy
# renders an inert "configure locally" state.
public/affordability.config.json
affordability.config.json
private/
